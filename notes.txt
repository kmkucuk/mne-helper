activate env
source ~/envs/data_science/bin/activate


install current (dir) package
pip install -e .


mne plots: need to be plot(block=True) otherwise plots are closed right away. 
dependencies for plots: pyvista pyvistaqt vtk qtpy ipympl ipywidgets trame_client trame_server trame_vtk trame_vuetify
